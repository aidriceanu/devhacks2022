import React from 'react';

export type StatsProps = {}

export default function Stats({}: StatsProps) {
    return (
        <div className="Stats">
            Stats are here
        </div>
    );
}
