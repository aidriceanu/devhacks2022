export enum ACTION {
    EVALUATION = "evaluation",
    SITUATION = "situation",
    ACTIVITY = "activity",
}

export enum Evaluator {
    TEACHER = "teacher",
    PARENT = "parent",
    SHADOW = "shadow",
    NANNY = "nanny",
    RELATIVE = "relative",
}